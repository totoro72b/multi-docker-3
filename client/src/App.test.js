
it('renders without crashing', () => {
  // comment out for now since it'll crash when trying to reach out to a server that doesn't exist
  // since we switched to using nginx for prod version

  // TODO: setup a faker module to mock out the request
  // const div = document.createElement('div');
  // ReactDOM.render(<App />, div);
  // ReactDOM.unmountComponentAtNode(div);
});
